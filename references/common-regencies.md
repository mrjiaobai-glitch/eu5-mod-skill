# common/regencies（摄政）

来源：`in_game\common\regencies\readme.txt`

## 字段

```
<regency id> = {
    start_effect = { <effects> }
    end_effect = { <effects> }
    allow = { <triggers> }
    modifier = { <modifiers> }
    internally_assigned = <yes/no>
}
```

## 审查要点

- 未在 readme 中说明：trigger/effect 作用域、本地化键格式。
